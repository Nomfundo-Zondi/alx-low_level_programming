#include "main.h"
/**
 * * main - check code
 * Return: Always 0. Success.
 */
int main(void)
{
	char *str;

	str = "I do not fear computs. I fear the lack of them - Isacc Asimov";
	print_rev(str);
	return (0);
}
